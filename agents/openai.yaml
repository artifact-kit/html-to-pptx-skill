interface:
  display_name: "HTML to PPTX JSX"
  short_description: "Create editable PPTX files from measured HTML"
  default_prompt: "Use $html-to-pptx-jsx to copy this HTML into a measured exporter and generate a downloadable, editable PPTX."
